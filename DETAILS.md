# 日期、关键词和正文处理

### 关键词与分类如何匹配

- 多个方向用中英文逗号分隔；1–12 个方向，每个最多 70 个字符。
- 方向之间是“或”，命中后按最高优先级归类。同一篇匹配多个方向只发一次；不是“分类内再用关键词筛选”。
- 普通关键词匹配论文标题和 abstract，不做语义检索。
- 推荐使用具体的英文研究术语。不自动把中文方向翻译为英文。
- 支持具体 arXiv 分类代码，如 `astro-ph.CO`、`astro-ph.GA`、`cs.AI`、`cs.LG`、`quant-ph`、`hep-th`。可直接输入，也可写成 `cat:astro-ph.CO`；大小写不敏感。
- 分类通过 API 的 `cat:` 字段检索，并核对论文分类元数据，包含主分类和交叉分类；标题或摘要里恰好出现代码不算分类命中。
- 官方别名统一为对应分类，例如 `cs.NA` → `math.NA`、`cs.SY` → `eess.SY`、`math.MP` → `math-ph`；同一分类的不同写法不会占多个优先级。
- 分类和关键词都适用 `/arxiv add`、`/arxiv remove`、`/arxiv priority`，例如 `/arxiv priority 1 astro-ph.CO`。原有关键词订阅继续使用；旧订阅中已填写的有效分类代码升级后按分类处理。
- 分类代码随版本维护，依据 [arXiv 官方分类表](https://arxiv.org/category_taxonomy)；当前快照核对于 2026-09-25。不支持 `cs.*` 等通配符、布尔表达式或任意 API 查询语法；无效的 `cat:` 选择器会明确报错。
- 太宽的关键词或分类组合可能超过每次查询的结果上限；程序会明确报告错误，不把截断结果当作完整日报。

## 一篇日报包含什么

1. 最高匹配优先级（如 `P1 · 21cm`）、论文标题、作者、arXiv 编号、首次提交日期和全部匹配方向。
2. 原始英文 abstract。
3. 可选概括：**研究空白 / 做了什么 / 怎么做的 / 结论**。
4. 论文页面和 PDF 链接。

概括依据论文正文文本，覆盖引言、方法、结果、讨论/结论和能够提取的附录；保留研究局限及数值限定，不用猜测补齐正文未说明的信息。消息会注明正文来源（HTML/PDF）、PDF 页数（如适用）、阅读分段数和固定版本链接。长消息会按微信文本限制拆成多段，保留完整 abstract。

### 如何读取论文

1. 先请求与元数据版本对应的 `https://arxiv.org/html/<id>vN`，提取论文主体、标题、公式文本和图表说明，排除网页导航。
2. HTML 缺失或无可用正文时，下载对应 PDF，使用 PDF.js 在独立工作线程中逐页提取文本。
3. 32,000 字符以内的正文直接作为概括依据；更长的正文**全部分段**阅读，先提取各段证据，再综合所有分段生成约 200 字/词的四项概括，不只截取开头。
4. 正文获取失败、某 PDF 页不可提取、超过大小/页数上限时，消息明确标为“未生成正文概括”，只保留原始英文 abstract 和链接。**不会用 abstract 概括冒充正文概括。**

这是正文**文本**阅读，不是对 PDF 版面的视觉审稿。图像本身不做视觉解读；图注和可提取的表格文本会进入输入，复杂公式、表格布局仍可能解析有误。模型生成内容也需要读者核对原文。arXiv 的 HTML 转换存在覆盖和渲染限制，见[官方说明](https://info.arxiv.org/about/accessible_HTML.html)。

当前上限为 HTML 10 MB、PDF 30 MB/300 页、提取文本 48 万字符；超限不会静默截断后宣称已读全文。PDF 解析最多 60 秒。正文失败缓存 30 分钟，避免重复请求；成功正文按论文编号和版本缓存，分段笔记也共享，同语言最终概括复用。长论文首次生成会增加模型调用次数和处理时间，后续同论文同语言请求可复用。

`/arxiv lang none` 不下载正文、不调用模型。已经发出“未生成正文概括”的论文仍计入发送记录，当前没有单篇重新生成或追加概括指令。


## 运行与数据

默认 `timeZone: "system"`，跟随运行 OpenClaw 的电脑时区，每日当地时间 08:00 开始。运行期间约每 30 秒重新读取电脑时区，系统切换时区后自动跟随；夏令时按当地规则处理。`/arxiv status` 会显示实际时区和当前论文日期范围。若 OpenClaw 在另一台电脑上运行，使用的是那台电脑的时区。

调度器每 30 秒检查一次，到设定时间后逐篇处理。首次概括长论文可能需要几分钟；08:00 是开始处理的时间。没有符合条件的新论文时不发空消息，可用 `/arxiv status` 查看。

电脑需要开机、联网、保持清醒，且 Gateway 正在运行。休眠期间无法推送；当日醒来后可处理当时的前一日，跨日不补历史。每次窗口都是当前有效时区前一个自然日的 `[00:00, 次日 00:00)`，依据 arXiv API `published`，不依据 `updated`、公告批次或本机抓取时间。晚入库的论文可能因此漏过，不扩大日期窗口补发。

SQLite 保存在 OpenClaw 状态目录的 `arxiv-daily/state.sqlite`。Zotero 加密密钥在同目录 `zotero-app.json`；恢复备份时需保留匹配的密钥。退订删除本人的订阅、投递和收藏绑定记录，微信聊天及通道日志仍由原系统保存。

插件只处理 OpenClaw 微信通道收到的消息，不读取你其他微信好友的聊天。插件启用时，会阻止这个 OpenClaw 微信通道中的普通消息启动 AI 对话。

升级会核对源码、备份配置并保留阅读记录；其他旧订阅保留在数据库中，个人版不调度它们。更换关键词或模型不重新发送历史论文。已生成的阅读笔记和概括会缓存，切换模型用于后续需要生成的概括。


## 时区变更与升级

从旧版升级时，未设置时区或旧默认 `Asia/Shanghai` 会改为 `system`；其他显式配置的固定 IANA 时区保留。仍需固定时区时，可将插件配置的 `timeZone` 设为例如 `Asia/Shanghai`。`system` 读取电脑的系统设置，不使用 Node 的固定 `TZ` 环境变量。

任务运行中检测到时区变化，会停止旧窗口的后续发送；下次定时或手动 `/arxiv now` 使用新时区的前一个自然日。同一本地日期的定时任务不会重复创建，已发过的论文仍会去重。时区切换、跨日和重试都不会扩大日期范围补历史。当天已经发起过定时任务后切换时区，可用 `/arxiv now` 手动处理新范围。

## 安装与排错

当前版本 **0.5.3**，适配 OpenClaw **2026.9.6**、腾讯微信插件 **2.4.8**。首次安装、模型登录和微信扫码需要本人完成；安装器不自动替换其他版本的 OpenClaw。

Gateway 启动超时不一定代表进程停止。在 Windows 运行 `node "$env:USERPROFILE\install-arxiv-daily.cjs" --status`，macOS 运行 `node "$HOME/install-arxiv-daily.cjs" --status` 检查。没有匹配论文时不会发送空日报；微信用 `/arxiv status` 查看任务结果。

GitHub Actions 在 Windows 和 macOS 上运行离线测试、安装器一致性检查和源码展开检查。测试不使用真实账号；真实登录、微信收件、Zotero 写入和无人值守运行需在自己的电脑确认。
